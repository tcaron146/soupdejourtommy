rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Public blog - all content is readable without auth
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}